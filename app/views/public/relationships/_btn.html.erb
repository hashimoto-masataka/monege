<% if current_user != user %>
  <% if current_user.following?(user) %>
    <%= link_to "参考から外す", user_relationships_path(user.id),remote: true, method: :delete, 'data-turbolinks': false ,class: "btn btn-info" %>
  <% else %>
    <%= link_to "参考にする", user_relationships_path(user.id), method: :post,remote: true,'data-turbolinks': false , class: "btn btn-success" %>
  <% end %>
 <% end %>